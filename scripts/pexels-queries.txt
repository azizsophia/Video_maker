# Scratch file for Pexels searches. Put --portrait (and --photos for covers)
# on the first non-comment lines, then one query per line, commit and push:
# the Pexels Search workflow prints candidates (id, dur, link, image) to its log.
